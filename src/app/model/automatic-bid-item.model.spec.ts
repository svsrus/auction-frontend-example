import { AutomaticBidItem } from './automatic-bid-item.model';

describe('AutomaticBidItem', () => {
  it('should create an instance', () => {
    expect(new AutomaticBidItem()).toBeTruthy();
  });
});
